pipeline {
    agent any 
    stages {
        stage('build') {
            steps {
                sh 'python --version'
                sh 'python3 --version'
                sh 'echo Vaishnav You are able run first jenkins job on your own'
            }
        stage('Html Page') {
            steps {
                sh 'This is a HTML PAge'
        }
    }
}
